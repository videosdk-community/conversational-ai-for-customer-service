
import asyncio, os
from videosdk.agents import Agent, AgentSession, CascadingPipeline, JobContext, RoomOptions, WorkerJob,ConversationFlow
from videosdk.plugins.silero import SileroVAD
from videosdk.plugins.turn_detector import TurnDetector, pre_download_model
from videosdk.plugins.deepgram import DeepgramSTT
from videosdk.plugins.openai import OpenAILLM
from videosdk.plugins.elevenlabs import ElevenLabsTTS
from typing import AsyncIterator

# Pre-downloading the Turn Detector model
pre_download_model()

class MyVoiceAgent(Agent):
    def __init__(self):
        super().__init__(instructions="""You are an expert conversational AI specialist dedicated to customer service excellence. Your primary role is to assist users with inquiries about implementing, optimizing, and managing conversational AI solutions for customer service environments. You are highly knowledgeable about chatbot design, natural language processing, AI integration, automation workflows, best practices for customer experience, and ethical considerations. Always maintain a clear, professional, and approachable tone. Communicate complex technical concepts in simple, actionable terms tailored to the user's level of expertise. Proactively offer detailed explanations, practical recommendations, and helpful resources. Your interaction style is empathetic, supportive, and focused on empowering users to harness conversational AI to improve customer satisfaction and operational efficiency. If asked about specific platforms, compliance, or integration scenarios, provide accurate, up-to-date guidance without making unsupported claims. Never share confidential business information or provide legal advice; instead, direct users to relevant official documentation or experts when necessary.""")
    async def on_enter(self): await self.session.say("Hello! How can I help you today regarding conversational ai for customer service?")
    async def on_exit(self): await self.session.say("Goodbye!")

async def start_session(context: JobContext):
    # Create agent and conversation flow
    agent = MyVoiceAgent()
    conversation_flow = ConversationFlow(agent)

    # Create pipeline
    pipeline = CascadingPipeline(
        stt=DeepgramSTT(model="nova-2", language="en"),
        llm=OpenAILLM(model="gpt-4o"),
        tts=ElevenLabsTTS(model="eleven_flash_v2_5"),
        vad=SileroVAD(threshold=0.35),
        turn_detector=TurnDetector(threshold=0.8)
    )

    session = AgentSession(
        agent=agent,
        pipeline=pipeline,
        conversation_flow=conversation_flow
    )

    try:
        await context.connect()
        await session.start()
        # Keep the session running until manually terminated
        await asyncio.Event().wait()
    finally:
        # Clean up resources when done
        await session.close()
        await context.shutdown()

def make_context() -> JobContext:
    room_options = RoomOptions(
     #  room_id="YOUR_MEETING_ID",  # Set to join a pre-created room; omit to auto-create
        name="VideoSDK Cascaded Agent for conversational ai for customer service",
        playground=True
    )

    return JobContext(room_options=room_options)

if __name__ == "__main__":
    job = WorkerJob(entrypoint=start_session, jobctx=make_context)
    job.start()
